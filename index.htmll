<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Pour toi Amy 🦋</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ffd1dc, #fff);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      font-family: system-ui, sans-serif;
      overflow: hidden;
      text-align: center;
    }

    h1 { font-size: 2.8rem; }
    h2 { margin-top: 10px; }

    button {
      padding: 15px 35px;
      font-size: 1.2rem;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      margin: 10px;
    }

    #yes {
      background: #ff4d6d;
      color: white;
    }

    #no {
      background: #ccc;
      position: absolute;
    }

    .butterfly {
      position: absolute;
      font-size: 24px;
      animation: fly 8s linear infinite;
      opacity: 0.9;
    }

    @keyframes fly {
      from {
        transform: translateY(110vh) translateX(0) rotate(0deg);
      }
      to {
        transform: translateY(-10vh) translateX(200px) rotate(360deg);
      }
    }
  </style>
</head>
<body>

  <h1>Hey Amy 🥺</h1>
  <h2>Tu veux être ma Valentine ? 💖</h2>

  <div>
    <button id="yes">Oui</button>
    <button id="no">Non</button>
  </div>

  <script>
    const noBtn = document.getElementById("no");
    noBtn.addEventListener("mouseover", () => {
      noBtn.style.left = Math.random() * 80 + "%";
      noBtn.style.top = Math.random() * 80 + "%";
    });

    document.getElementById("yes").addEventListener("click", () => {
      document.body.innerHTML = "<h1>c’est vrai ça 🙂 ?</h1>";
      for (let i = 0; i < 30; i++) {
        createButterfly();
      }
    });

    function createButterfly() {
      const butterfly = document.createElement("div");
      butterfly.className = "butterfly";
      const colors = ["🦋","❤️","🦋","🦋"];
      butterfly.textContent = colors[Math.floor(Math.random() * colors.length)];
      butterfly.style.left = Math.random() * 100 + "vw";
      butterfly.style.animationDuration = (5 + Math.random() * 5) + "s";
      document.body.appendChild(butterfly);

      setTimeout(() => butterfly.remove(), 10000);
    }
  </script>

</body>
  </html>
